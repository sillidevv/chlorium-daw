# + CHLORIUM +
`[Placeholder]`


### TODO:
**Ordererd after priority*
- Readme

- Make a `Window` trait with `draw(ctx: &egui::Context)`
  and `draw_content(ui: &mut egui::Ui)`

- Finish the "About Chlorium" window

- Remake tracker channel system into a more grid based type of 
  a tracker interface

